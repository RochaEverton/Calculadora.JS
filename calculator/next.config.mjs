/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: {
    styledComponents: true, // habilita suporte oficial ao styled-components
  },
};

export default nextConfig;
